# 简化的AI饮食助手配置
port=8080
static_dir=./frontend
python_script=../AI_Demo/main.py

# AI模型配置
# primary_model: ollama / deepseek / llamacpp
# llamacpp 的 gpu_layers: -1=全部GPU, 0=CPU, N=前N层
primary_model=ollama
primary_model_name=deepseek-r1:7b
secondary_model=deepseek
secondary_model_name=deepseek-chat
ollama_server=http://localhost:11434
# llama.cpp 服务器地址（需用户自行启动 llama-server）
# 启动命令: llama-server -m 模型路径 -ngl -1 --host 127.0.0.1 --port 11435
llamacpp_server=http://127.0.0.1:11435